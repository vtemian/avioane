// Generated by CoffeeScript 1.3.3
(function() {
  var Game, Map;

  Map = (function() {

    function Map(options) {
      this.options = options;
      this.m_canvas = document.createElement('canvas');
      this.m_canvas.width = this.options.width;
      this.m_canvas.height = this.options.height;
      $('#initial-game').append(this.m_canvas);
      this.draw_map();
    }

    Map.prototype.draw_map = function() {
      var context, imageObj;
      context = this.m_canvas.getContext("2d");
      imageObj = new Image();
      imageObj.onload = function() {
        return context.drawImage(imageObj, 69, 50);
      };
      return imageObj.src = "http://www.html5canvastutorials.com/demos/assets/darth-vader.jpg";
    };

    return Map;

  })();

  Game = (function() {

    function Game(firstOponent, secondOponent) {
      this.firstOponent = firstOponent;
      this.secondOponent = secondOponent;
      this.map = new Map({
        'width': 1000,
        'height': 3000
      });
    }

    return Game;

  })();

  $(document).ready(function() {
    var game;
    game = new Game('george', 'asd');
    return console.log(game);
  });

}).call(this);
